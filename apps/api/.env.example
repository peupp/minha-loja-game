# SQLite — arquivo local, não precisa instalar PostgreSQL
DATABASE_URL="file:./dev.db"

PORT=3001
PUBLIC_URL="http://localhost:5173"

# Opcional: PostgreSQL (Docker, Supabase, Neon…) — troque provider no schema.prisma para postgresql
# DATABASE_URL="postgresql://postgres:postgres@localhost:5432/minha_loja?schema=public"
